web_player/node_modules/
web_player/dist/
web_player/.env.local

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Caches de herramientas
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Secrets / env
.env
.env.*
# …but allow placeholder templates (no real secrets) to be versioned:
!.env.example
!.env.*.example

# Claude Code — config local del agente (contiene SSH password en settings.json)
.claude/

# Deploy scripts con rutas/info del servidor
mcp_server/_check_build.py
.ssh_known_hosts
